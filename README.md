# dual_heater_pid
The dual heater runs PID and auto-calibration on the klipper system

Prebuilt `.so` binaries for ARM64 + Python 3.9.

## Deploy

```bash
git clone https://github.com/monodgi/dual_heater_pid.git dual_heater_tmp
bash dual_heater_tmp/install.sh
